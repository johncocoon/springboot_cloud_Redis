<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atguigu.atcrowdfunding.manager.mapper.RoleMapper" >
  <resultMap id="BaseResultMap" type="com.atguigu.atcrowdfunding.bean.Role" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="name" property="name" jdbcType="VARCHAR" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from t_role
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.atguigu.atcrowdfunding.bean.Role" >
    insert into t_role (id, name)
    values (#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.atguigu.atcrowdfunding.bean.Role" >
    update t_role
    set name = #{name,jdbcType=VARCHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select id, name
    from t_role
    where id = #{id,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Thu Mar 01 09:47:00 CST 2018.
    -->
    select id, name
    from t_role   
    <where >
    		<if test="name !=null and name !=''">
    				name like concat('%',#{name},'%')
    		</if>
    </where>
  </select>
  
  
  <insert id="insertByUseridAndRoleId" parameterType="com.atguigu.atcrowdfunding.bean.Role" >
   
    insert into t_user_role (userid, roleid)
    values 
    	<foreach collection="ids"  separator="," item="id">
					(#{userid},#{id})
	    </foreach>
    
  </insert>
  
 
  <delete id="deleteRoleById"  >
    delete from t_user_role
    where userid = #{userid,jdbcType=INTEGER} and roleid in
    <foreach collection="ids" open="(" close=")" separator="," item="id">
					#{id}
	    </foreach>
  </delete>
  
  <insert id="insertPermissionRole"  >
   
    insert into t_role_permission(roleid, permissionid)
    values 
    	<foreach collection="ids"  separator="," item="permissionid">
					(#{roleid},#{permissionid})
	    </foreach>
    
  </insert>
  
  
   <delete id="deletePermissionRole"  >
    delete from t_role_permission
    where roleid = #{roleid,jdbcType=INTEGER}
  </delete>
  
  
  <select id="queryByRoleid" resultType="java.lang.Integer">
  			SELECT  t_permission.id FROM t_permission,t_role,t_role_permission
			WHERE  t_role.id=#{roleid} AND
			t_role.id = t_role_permission.roleid  AND 
			t_role_permission.permissionid = t_permission.id
  </select>
  
  
</mapper>